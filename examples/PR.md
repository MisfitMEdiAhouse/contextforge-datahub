# ContextForge impact-aware rename

DataHub evidence: PII + Tier1, owned by data-platform, with three downstream assets.
Generated migration keeps a compatibility alias, preserves governance metadata, and requires human approval before merge.
