# ContextForge — final <3-minute demo recording plan

Target runtime: **2:30–2:50**. Hard stop before 3:00.

The live app already contains the narration. Do not add a second voice-over and do not use the old prerecorded MP3 narration.

## 0:00–0:25 — Establish scope

Start recording at the **top of the live app** with device/browser volume on.

Hold briefly on the hero so these ideas are readable:

- ContextForge checks what a data change could break before an AI coding agent writes or ships it.
- DataHub context → impact policy → code + tests → human gate → DataHub memory.

Then scroll steadily through the application. Let the viewer see the change request, five context signals, decision trace, generated-output area, human gate, architecture/value sections, and the final Judge Takeaway. Do not stop to manually operate controls during this preview.

## 0:25–0:35 — Lock the takeaway before the guided run

At **Judge Takeaway**, briefly hold on:

- BEFORE: Prompt → code → deploy → discover damage
- WITH CONTEXTFORGE: Understand → assess impact → generate → govern → approve → remember
- 5 context signals → 3 generated artifacts → 1 safety boundary → auditable decision memory

Press the **bottom Start judge-ready guided demo** button. It launches the same Judge Mode path as the top button.

## 0:35–2:35 — Run Judge Mode straight through

Judge Mode contains 14 synchronized steps. The public action stays **NEXT** so the viewer never needs to understand internal demo controls.

1. Why prompt-only agents are dangerous
2. PII rename / scenario breadth
3. Type migration
4. Deprecation
5. DataHub context: schema, lineage, ownership, governance, usage
6. Evidence → impact → policy → Safety Gate
7. Generated dbt model
8. Generated schema tests
9. Evidence-linked PR summary + human approval boundary
10. ContextForge Run Report
11. Real DataHub MCP `save_document` write-back contract
12. Context memory / next agent inherits the approved decision
13. Business value: DataHub as context + control layer
14. Expansion pattern / final platform takeaway

Click **NEXT** once per step after each narration finishes. Do not manually click scenario buttons, tabs, Analyze, Approve, or memory controls; Judge Mode performs those underlying actions automatically.

At Step 14 the control becomes **Restart demo**. Do not restart during the submission recording.

## 2:35–2:50 — Finish on the value proposition

End with the final visible message and no extra wandering:

**DataHub becomes an active control layer for AI coding agents.**

ContextForge uses DataHub metadata before generation, requires a human boundary for risky changes, and preserves approved decisions as reusable context for the next agent.

## Truth boundaries for the recording

- Browser demo uses deterministic DataHub-shaped fixture data and local browser memory.
- The public CLI implements the real DataHub MCP read path and explicit approved `save_document` write-back path.
- Do not imply the browser performed a live DataHub mutation.
- Cybersecurity and gaming are architecture expansion paths, not current integrations.
- No proprietary GHOSBC source is included in the hackathon project.
